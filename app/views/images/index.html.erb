<div class="page-header"><h1>Explore Flickr</h1></div>
  <% @images.each do |image| %>
  <div class="col-sm-6">
    <%= image_tag("#{image.image_file_name}") %>
    <!-- <%= form_for image do |f| %>
      <%= f.radio_button :users_who_favorited, true %>
      <%= f.label :favorite_true, 'Like'%>
      <%= f.radio_button :users_who_favorited, false %>
      <%= f.label :favorite_false, 'Unlike'%>
      <%= f.submit class: 'btn btn-default' %>
    <% end %> -->
    <hr>
  </div>
  <% end %>
</div>
